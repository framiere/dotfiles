#!/bin/sh
#

BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null)
if [ $? -ne 0 ]; then
    echo "Not a git repository"
    exit 1;
fi

git fetch
git wip
git rebase --stat origin/${BRANCH}
git unwip
git heads
